# 版本鎖定在 2026-09-10 實測可正常運作的組合（Python 3.11）。
# streamlit 已移除：它以前只被用來提供 market.py 的快取裝飾器，
# 卻連帶拉進 pyarrow/pydeck/altair 近 200MB。現在用自寫的 cache_data。
# 不鎖的話 Render 每次重新部署都會抓當下最新版，上游一個 breaking change
# 就會讓線上版無預警壞掉。要升級時手動改版號，本機測過再推。
yfinance==1.7.0
pandas==3.0.5
openpyxl==3.1.5
lxml==6.1.3
requests==2.34.2
